rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 移行中のみ：認証済みユーザーに全アクセスを許可
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
